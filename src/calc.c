#include "calc.h"
int calc::sum(int a, int b){
return (a+b);
}
int calc::diff(int c, int d){
return (c-d);
}

int calc::div(int e, int f){
return (e/f);
}
int calc::mul(int g, int h){
return (g*h);
}
